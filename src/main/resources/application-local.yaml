spring:

  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/sylvie

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop